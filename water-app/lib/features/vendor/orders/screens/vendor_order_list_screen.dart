import 'package:flutter/material.dart';

class VendorOrderListScreen extends StatelessWidget {
  const VendorOrderListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('VendorOrderListScreen')));
  }
}
